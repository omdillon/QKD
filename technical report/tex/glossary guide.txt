CommandOutput\gls{qkd}First use: Quantum Key Distribution (QKD); all subsequent uses: QKD\Gls{qubit}Capitalised first letter — use at the start of a sentence\glspl{qubit}Plural form: qubits\glsreset{qkd}Forces the next \gls{qkd} to behave as a first use again (useful after an abstract)\glsdesc{entanglement}Prints only the description, not the term — useful in captions\gls{sym:rsec}Renders the symbol ( R_\mathrm{sec} ) with a hyperlink to the nomenclature list